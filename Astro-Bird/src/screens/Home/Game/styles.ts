import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  engineContainer: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0
  },
  scoreText: {
    fontSize: 60,
    position: 'absolute',
    top: 80,
    left: 170,
    color: 'white',
    fontWeight: 'bold',
    fontFamily: 'sans-serif-light',
  }
});
